#include "list_algos.h"
#include "dbg.h"

int List_bubble_sort(List * list, List_compare passedfunc) {
    check(list != NULL, "List to be sorted must exist");

/* 
 * TODO
 *
 * */
    return 0;

error:
    return 0;
}

List* List_merge_sort(List * list, List_compare passedfunc) {
    check(list != NULL, "List to be sorted must exist");

/* 
 * TODO
 *
 * */

error:
    return list;
}
